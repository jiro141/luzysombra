package com.luzysombra.navigation;

/**
 * Tipos de pantalla del juego. Todas se muestran dentro del mismo Stage
 * principal (StackPane del ScreenManager); nunca se abren ventanas nuevas.
 */
public enum ScreenType {
    MAIN_MENU,
    TUTORIAL,
    LEVEL_SELECTION,
    GAME
}
